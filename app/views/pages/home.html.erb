<div>
    <div>
        <h1 class="font-weight-bold text-center">Bienvenido a SSCatFacts <h1>
        <h3 class="font-weight-normal text-center">
            Aquí podrás aprender más de tu gato a través de nuestros datos o
            <code>#fact</code>.
        </h3>
    </div>
    <% if current_user %>
        <div class="text-center">
            <a href="<%= url_for(fact_cats_path) %>" class="btn btn-primary btn-lg" role="button" aria-disabled="true">!Pincha aqui para conocer más datos! <i class="fas fa-cat"></i></a>
        </div>
        <br>
        <h2 class="font-weight-bold text-center">Facts más populares</h2>
    <% else %>
        <h3 class="font-weight-normal text-center">
            ups! no has iniciado sesión
        </h3>
        <div class="text-center">
            <a href="<%= url_for(user_session_path) %>" class="btn btn-primary btn-lg" role="button" aria-disabled="true">Iniciar sesión <i class="fas fa-cat"></i></a>
            <br/>
            <br/>

            <a href="<%= url_for(new_user_session_path) %>" class="btn btn-primary btn-lg" role="button" aria-disabled="true">Registrarse<i class="fas fa-cat"></i></a>
        </div>
    <% end %>
</div>
